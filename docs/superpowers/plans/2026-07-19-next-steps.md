# План следующих шагов (после red-team волны 2026-07-19)

Статус: этап «парсер-машина» завершён в основной части — 247 тестов, ежедневный цикл работает, ревью-волна проверена на реальных данных (HANDOFF 2026-07-19). Этот план — очередь на продолжение.

## 0. Первое действие пользователя (без него отчёты пустые)

- [ ] Свежий сбор: `Поиск_RTS_другие_площадки.bat` (и/или `Автосбор_RTS_кабинета.bat`), полистать выдачу, затем `Отчет_по_накопленному_RTS.bat`. Все накопленные карточки от 03.07 просрочены.
- [ ] Во время сбора — live-проверка починенного watcher'а: бейдж на странице, `+N новых` в консоли, реакция на закрытие/открытие Chrome (reconnect каждые 5 с), сообщение при занятой БД.

## 1. Бэклог качества парсинга (по убыванию ценности)

- [ ] **Анти-«химера»**: перед `evaluate_tender` подмешивать в карточку непустые поля из БД (price/deadline/customer/region по `unique_key`), чтобы экспорт совпадал с merged-строкой и hot-карточка не уходила оператору без цены, которую БД знает. Реализация: `TenderStorage.merge_with_history(tenders)` между enrichment и dedup в `cli.run`; TDD в `tests/test_storage.py` + cli-тест.
- [ ] **Dedup по номерам**: pre-pass слияния записей с одинаковым `tender_number` длиной ≥8 между источниками (приоритет по `SOURCE_PRIORITY`); плюс guard от слияния РАЗНЫХ тендеров с одинаковым названием+ценой+днём — не сливать при непересекающихся заказчиках (токен len≥5). Риск регрессии — писать тесты на оба направления до кода.
- [ ] «щит»→«информационные щиты» (благоустройство) — принятый риск; вернуться, если появится в горячих на реальных данных.

## 2. Источники (по мере необходимости)

- [ ] ЕИС open-data (ftp.zakupki.gov.ru / SOAP) вместо HTML — отдельное исследование; HTML с `recordsPerPage=_50` работает (live-проверено 03.07).
- [ ] Вернуть ЕИС в ежедневный `fast`-профиль после стабилизации сети.
- [ ] Живая проверка формата дат poisk-страницы после обновлений RTS (ISO-fallback уже поддержан).

## 3. Этап 2 роадмапа — Windows-приложение (по команде пользователя)

- [ ] Спека: окна (очередь «Новые/Горячие» с отметками, запуск сбора, статус watcher'а, настройки словарей), стек (предложить: Python + PySide6 или Tauri поверх `latest.json`/SQLite), упаковка (PyInstaller). Ядро не трогать — GUI поверх `tender_parser` как библиотеки.
- [ ] После спеки — план по конвенции `docs/superpowers/plans/`.

## Инварианты (не забывать)

- Push всегда в remote `target` (`flycited2-dotcom/tender-parser`), ветка `codex/rts-tender-parser`.
- Сеть машины не достигает `*.rts-tender.ru` — всё RTS только через Chrome пользователя (CDP 127.0.0.1:9222).
- TDD: перед сдачей полный `python -m pytest -q` (сейчас 247 passed).
- Журнал сессий — `docs/HANDOFF.md`; постоянная память — в Claude memory (workflow + roadmap).
